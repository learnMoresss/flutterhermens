# 本机 SSH 部署用环境变量示例（复制到 ~/.hermes-remote.env，chmod 600，勿提交）
# 仓库内 scripts/ 为本地工具目录，已 gitignore。

export HERMES_HOST=your.server.ip
export HERMES_USER=ubuntu

# 密码登录须安装 sshpass；勿把真实密码写入仓库
# export HERMES_SSH_PASS='your_password'

# 公钥免密（推荐）
# export HERMES_SSH_KEY="$HOME/.ssh/id_ed25519"
